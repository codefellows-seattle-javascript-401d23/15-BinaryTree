'use strict';

import BinaryTree from '../lib/binary-tree';

describe('traversals.js', () => {
  test('should return array with values in correct order', () => {
    expect(BinaryTree(1)).toEqual(1);
  });
});
